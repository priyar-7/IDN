What is IDN?

IdentityNow = A cloud system that manages and controls access for all employees.

“IdentityNow is a cloud-based SaaS Identity Governance and Administration (IGA) platform that manages user identities, automates provisioning, and ensures secure access across applications.”

✅ IdentityNow (IDN) is a cloud-based SaaS.

Here’s the simplest meaning:

Cloud-based → It runs on the cloud (no servers needed).

SaaS (Software as a Service) → You just use it like a service; SailPoint manages all backend, servers, upgrades, patches.

📌 So IDN = Cloud-based SaaS platform for

Managing user identities

Managing access

Automated provisioning

Governance (reviews, certifications)
